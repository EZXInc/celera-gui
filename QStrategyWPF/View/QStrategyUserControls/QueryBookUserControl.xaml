<UserControl x:Class="QStrategyWPF.View.QStrategyUserControls.QueryBookUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Expander Header="QUERY BOOK" BorderThickness="1" BorderBrush="LightBlue"  IsExpanded="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Symbol"/>
                    <TextBox x:Name="TxtSymbol" Width="100" Height="25"  
                              />
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Book Info"/>
                    <TextBox x:Name="TxtStrategyConfig" Height="25" IsReadOnly="True" Background="{StaticResource ControlBackgroundBrush}"
                        TextWrapping="Wrap" Text="" />
                </StackPanel>
            </Grid>
        </Expander>
    </Grid>
</UserControl>
